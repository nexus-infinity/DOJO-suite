# DOJO Today production-surface boundary pass

**Object:** `DOJOTodaySurfaceView` and the existing `GeneratedObjectShell` store contract — place navigation, selected-object scope, composer-mode intent, and truthful object homes.

**Intention:** Make the current surface more production-usable without collapsing wireframe infrastructure, processing, and experience.

## Lens landing

- `Morphosis paired-move proof`: no authoritative data residence changed. The work remained in the DOJO Suite source and receipt homes; the graphical correction was an experience-layer change, not a move of objects, registries, or chamber state.
- `Do-Re-Mi lens doing`: **DO** grounded the rebuilt app, source layout, measured capacities, and live 7410 result; **RE** preserved the three channels and the distinct DOJO/SOMA phenotypes/homes; **MI** made only the leading-edge alignment and accessibility scroll seam, then re-built, re-witnessed, and recorded the result.
- `One FIELD, not one surface`: the Suite carries the DOJO mirror-portal role while SOMA remains a separate sovereign role. Shared rhythm does not create identity equivalence or authority transfer.

## Observer

- The rebuilt `DOJOApp` opened the `DOJO Today` window and exposed all six existing places plus the utility dock.
- Before this pass, opening a document and selecting Media changed the breadcrumb but left the document body and Details state visible.
- Before this pass, Review mode could fall through to `submitHostedIntent()` when text was present.
- Before this pass, most visible places read the same global object list; the rail could look complete without a stored home for the object.

## Weaver

- Existing place buttons now route through `selectPlace(_:)`.
- A selected object is retained only when the destination place contains its existing representation; otherwise the object and contextual dock return to the horizon.
- Review mode opens Review and reports the next lawful mode (`Ask` or `Capture`) without entering the hosted path.
- The existing object shell now carries `GeneratedObjectHome`; Today remains the overview, while the other existing places filter by that home. Older stored objects decode with a deterministic kind-based fallback.
- No new store, registry, chamber, model layer, or combined Current Evaluation surface was created.

## Evidence

- Xcode build: `xcodebuild -project DOJO-suite.xcodeproj -scheme DOJOApp -configuration Debug CODE_SIGNING_ALLOWED=NO build` — `BUILD SUCCEEDED`.
- Focused tests: `swift test --disable-sandbox --filter TodayAttention --filter DojoRenderProjectionV1Tests` — 15 passed, 0 failures.
- Runtime witness: document opened → Media selected → document cleared, Details closed, status `Moved to Media.`
- Runtime witness: Review selected → text entered → Send pressed → status `Review mode does not send to a hosted model...` and no answer object or API-key route was created.
- Runtime witness: the rebuilt app opened `Threads` with no answer objects and opened `Documents` with the persisted migration-gap document; Details reported `Home Documents`.
- Runtime witness: the rebuilt Today shell visibly identified itself as `Mirror portal · DOJO spinning top`; its accessibility help stated that the surface is not source-of-truth authority.
- Surface witness: the composer now reserves visible voice and attachment affordances as non-interactive `HOLD` presentation states; they do not enter processing or imply a live input path.

## Surface capacity weave

- `TodaySurfaceCapacity` is a presentation-only classifier inside `DOJOTodaySurfaceView`; its candidate `expansive`, `working`, and `compact` breakpoints describe available surface capacity, not device identity or processing state.
- In compact capacity, the centre remains dominant and the contextual utility dock returns to its horizon. An explicit Tools action reopens Details / Review / Files; object selection does not silently consume the centre.
- Compact hosted Ask controls compress behind one explicit provider/model menu. Object actions use the existing labels when they fit and icon-only affordances when they do not.
- The existing developer-only Boundary test toggle exposes measured dimensions and posture without presenting those measurements as processing, authority, or user-facing meaning.
- No processing, routing, persistence, registry, chamber, or authority seam changed in this capacity pass.

## Capacity evidence

- Working-capacity witness: Boundary test reported `1,180 × 791`, left `188`, centre `720`, right `272`, composer open with full provider/model controls; the overlay explicitly identified itself as developer measurement only.
- Compact-capacity witness: rebuilt surface rendered at approximately `824 × 791` with the centre dominant, compact mirror-portal identity, visible HOLD affordances, and the contextual dock at the horizon.
- Compact utility witness: explicit Tools reopened the utility dock and exposed Details / Review / Files while the centre object/processing route remained unchanged.
- Accessibility specimen witness: the existing Boundary test instrument applied the accessibility enlargement posture at compact capacity and exposed it in the measured surface label; the accessibility control was reachable from the developer-only status popover.

## Qualification

- This proves three corrected local seams, not full industry-benchmark completion. The graphical surface is now coherent at the shell level, but remains a local workbench rather than a fully integrated production suite.
- Dedicated per-place stores, live MCP, memory, live Arkadaş continuity/disagreement processing, attachment/mic execution, measured attention benefit, and an independent External Observer Seal remain `HOLD`.
- Residence remains local to the DOJO Suite worktree; the current King’s Chamber packet still records the broader mapping as `HOLD` / intentionally unsealed.
- The portal identity seam is a presentation contract only. It does not prove a live spinning-top round-trip; that remains a separate functional witness.
- The compact screenshot and accessibility specimen are runtime witnesses of the rebuilt surface, not an industry-benchmark certification. Compact accessibility enlargement is now visibly runtime-witnessed; the full cross-capacity typography/reflow comparison remains `HOLD`.
- Functional witness: Today `DOJO` mode sent one bounded message through `SpinningTopClient` to `127.0.0.1:7410`, received HTTP success, created an Answer object in `Threads`, and Details preserved `Provider DOJO spinning top` / `Home Threads`.
- Functional qualification: the returned response reported `inference unavailable`; this proves portal transport and object placement, not successful model inference or authority.

## Channel separation and current functionality witness

- The channel map is recorded in `docs/DOJO_TODAY_CHANNEL_SEPARATION_MAP_V0.md`.
- A fresh compact runtime exercise sent `Surface seam test. Reply with READY only.` through the existing DOJO mode. The surface reported `DOJO portal response ready · phi4:14b-q4_K_M`, created an Answer object in Threads, and opened its object context.
- The object body truthfully reported `[DOJO 7410 ✓ — inference unavailable] Niama 20B and Ollama both unreachable. character=padawan.`
- This proves the existing experience → transport → local object seam and its fail-visible error path. It does not prove model inference, Arkadaş continuity/disagreement, evidence sealing, live recursive spin, or authority promotion.
- Wireframe capacity and processing behavior remain separate: compact layout changed the presentation allocation only; the DOJO request used the existing client and object materialisation path.

- Fresh bounded UI witness: a request reached the existing DOJO route but timed out before a response; the surface reported `Blocked` / `DOJO portal unavailable` and did not create a false answer object. This is a processing HOLD, not a successful inference witness.
- Experience correction after the initial witness: the empty work surface now remains open at larger capacities and retains a bounded grouping only at compact capacity; ordinary use inherits the user's native macOS dynamic-type setting instead of forcing `.large`. The compact screenshot remains the current runtime capacity witness; larger-capacity visual comparison is still required.

## Processing/runtime correction

- The existing canonical DOJO 7410 server had a stale hard-coded Ollama target, `phi4:14b-q4_K_M`, absent from the current local inventory. The processing lane now uses the existing bounded model configuration seam and reports `status` / `inference_available` separately from transport. The installed generic model route is not proof that the canonical DOJO `openai/gpt-oss-20b` model is seated.
- The existing `com.field.mcp.dojo` launchd job was restarted; `/health` returned `200` and `status: healthy`.
- The bounded model request still did not return within the test window; real inference remains `HOLD`.
- No surface, object schema, registry, chamber, or authority behavior was changed by this correction.

- Identity alignment: the existing `Portal.dojoSuite` declaration and its test now preserve the shared DOJO/SOMA pulse genotype while keeping DOJO Suite and sovereign SOMA as distinct phenotypes and homes.

- Final experience alignment witness: after rebuilding and refreshing the runtime, the expansive specimen measured `1,920 × 1,048` with `left 188 · centre 1,680 · right 52`; the hero remained open (no grouped card) and its content aligned to the centre canvas leading edge, matching the supplied quiet production surface. Compact remains grouped and centre-dominant. This is a local graphical witness, not an industry-benchmark certification.

- Accessibility follow-up witness: refreshed compact mode at `960 × 620` exposed the Today centre as a vertical scroll area under accessibility enlargement; the fixed composer and rails remained outside that scroll seam. Enlarged content is therefore reachable rather than silently clipped. Comparative cross-capacity accessibility proof remains `HOLD`.

- Post-refresh functionality witness: a bounded composer request reached the existing DOJO 7410 route and timed out; the rebuilt surface showed `Blocked` / `DOJO portal unavailable`, preserved the input path as the DOJO mirror portal, and did not materialise a false Answer object. The test state was then dismissed and the composer returned to empty/Ready.

**Decision:** `PROMOTE` for the bounded local UI interaction and capacity seams; `HOLD` for full responsive proof, empirical attention benefit, successful model inference, broader runtime, and external alignment claims.

## Post-pass processing boundary correction — 2026-08-29T1726+1000

- The earlier transport witness above was recorded before the client-side inference gate was closed. Its Answer object is historical evidence of the former behaviour, not current behaviour.
- `submitDojoPortalIntent` now returns a visible `DOJO portal reached · inference HOLD` state when the typed response reports `inference_available: false`; it does not create or select a Thread Answer, clear the composer, or open object context.
- Focused tests now cover explicit unavailable inference, compatibility responses with no availability field, the legacy unavailable-response marker, and the typed Today processing packet: 41 tests passed, 0 failures.
- Current qualification: transport reachability, inference readiness, object materialisation, and authority remain separate. Successful inference and a live recursive round-trip remain `HOLD`.

## Processing contract seam — 2026-08-29T1731+1000

- The missing processing seam is now represented by `Sources/DOJOShared/Today/TodayProcessingPacket.swift`.
- It keeps current object, intention, intended sequence, minimum steps, evaluation question, unresolved decision, evidence state, and Arkadaş continuity/disagreement as separate fields.
- `HOLD` and `Unknown` remain explicit; `mayProjectResolvedState` blocks resolved presentation when evidence is insufficient or disagreement is detected.
- The contract has no runtime producer and no Today view consumer yet. This is processing-channel progress, not a claim that the graphical surface has acquired processing meaning.
- Three contract tests plus the existing surface/identity tests now pass: 41 focused tests, 0 failures. The app build remains successful.

## Mirror identity affordance correction — 2026-08-29T1744+1000

- The mirror-portal chip previously used a sync symbol that the accessibility tree exposed as `Syncing`, even when no live transport was active.
- The experience layer now uses a neutral mirror symbol (`rectangle.on.rectangle`) while retaining the explicit `Mirror portal · DOJO spinning top` label and separate SOMA explanation.
- This is an experience-only correction. It changes no route, processing packet, persistence, identity registry, or authority state.
- Live service qualification remains `HOLD`: the canonical launchd job is present but the DOJO child is not currently listening on 7410; Ollama is also not listening on 11434.

## Co-present phenotype boundary — 2026-08-29T1753+1000

- The identity seam now records the clarified relationship: DOJO Suite is the sacred-geometry genotype expressed as a working DOJO mirror phenotype; SOMA is a co-present sovereign peer phenotype with a separate lawful home.
- The existing Connections disclosure presents both lanes and states that shared pulse genotype does not merge identity, authority, route, or home.
- This acknowledges field-level co-presence without claiming a live DOJO↔SOMA route, sync, embodiment, or authority relationship. A specific live relationship remains `Unknown` / `HOLD` until separately witnessed.
- Focused identity and surface tests: 42 passed, 0 failures; Xcode build succeeded. The external observer seal and canonical DOJO inference remain `HOLD`.

## Canonical DOJO functionality re-witness — 2026-08-29T1804+1000

- Existing canonical launchd job `com.field.mcp.dojo` was restarted with `launchctl kickstart -k`; no launchd configuration or source route was changed.
- Direct canonical probes then agreed: 7410 `/health` was healthy, `/state` reported `operational: true`, and `/models` exposed the existing local inventory.
- A bounded `/chat` request returned a typed completion: `status: ok`, `inference_available: true`, model `gemma3:latest`, response `READY`, and `cm4_preflight_verdict: ALLOW`.
- The rebuilt Today surface completed the same path end-to-end: composer → canonical DOJO route → inference → unsaved `Answer` object. Details reported provider `DOJO spinning top`, model `gemma3:latest`, and response body `READY`.
- The test object was closed without saving. No persistent object, registry, or authority state was created by the witness.
- This promotes one witnessed local completion. It does not promote canonical `openai/gpt-oss-20b` seating, repeatable performance, runtime production/consumption of the full Today processing packet, Arkadaş continuity/disagreement, recursive spin, or the External Orbiting Observer Seal.

## Window continuity correction — 2026-08-29T1809+1000

- Added the experience-only `DOJOWindowPresentationGuard` to the existing Today surface.
- On first window attachment, restored full-screen state is cleared so a rebuilt/relaunched iteration returns to the ordinary working frame.
- Deliberate user full-screen use remains available; the guard does not alter capacity, processing, object state, persistence, identity, or authority.
- Rebuilt `DOJOApp` successfully and relaunched the built binary. The accessibility witness reported a standard `DOJO Today` window with the full-screen control available and no automatic full-screen takeover.

## Compact capacity interpretation witness — 2026-08-29T1814+1000

- The existing developer-only Boundary test measured the current window at `960 × 620`: left `188`, centre `720`, right `52`.
- This is the existing compact capacity, so the grouped centre card is consistent with the surface contract. It is not evidence of a working-capacity layout failure.
- The measurement overlay was disabled afterward; the ordinary Today surface returned to its quiet state.

## Working-capacity witness boundary — 2026-08-29T1818+1000

- A temporary request to resize the existing window to `1180 × 820` was constrained by the current display to `960` points wide.
- The app consequently remained in its compact capacity. No working-capacity visual comparison was claimed or inferred from this run.
- The window was restored to `960 × 652`; the developer measurement overlay remained off.

## Quiet-surface accessibility correction — 2026-08-29T1822+1000

- The Today centre's decorative place symbol was being exposed to accessibility as “Brightness Higher,” despite the adjacent “Today” title already carrying the meaning.
- Added `.accessibilityHidden(true)` to that decorative image only. No visible geometry, processing state, object identity, persistence, or authority behavior changed.
- Rebuilt and relaunched `DOJOApp`. Runtime accessibility verification retained `Today READY TO BEGIN` and `Mirror portal · DOJO spinning top`, while `Brightness Higher` was absent.

## King’s Chamber lens alignment pass — 2026-08-29T1826+1000

- Rechecked the existing Morphosis, DoReMi, and OAW artifacts against the current DOJO Today map.
- Morphosis supports the present seating: DOJO Suite remains a DOJO spinning-top mirror phenotype, SOMA remains a separate sovereign phenotype/home, and the runtime seal is still held.
- DoReMi remains the full octave-return sequence, not a reduced three-step mechanic. The current surface has partial declare/observe/measure/form/test evidence; dynamic seal/return remains held.
- OAW remains the governing execution sequence. The lens artifacts enrich evaluation but do not become wireframe, processing, experience, registry, or authority.
- No new registry, chamber, model layer, or replacement architecture was created.

## Xcode target visibility correction — 2026-08-29T1834+1000

- The first post-seam Xcode build exposed one concrete project-visibility gap: `TodayProcessingPacket.swift` was present in the Swift package but absent from the DOJOShared Xcode target, so the app target could not resolve the type.
- Added only the existing source-file reference, Today group membership, and DOJOShared Sources build entry in `DOJO-suite.xcodeproj/project.pbxproj`.
- No processing fields, UI structure, persistence, identity, route, or authority behavior changed.
- Xcode build now succeeds; focused `TodayProcessingPacket` tests pass 4/4; `git diff --check` passes.
- Runtime re-witness is intentionally pending because macOS is currently locked and the desktop-control surface cannot unlock it automatically. No claim is made about the new packet being visible at runtime until the Mac is manually unlocked.

## Unlocked runtime re-witness — 2026-08-29T2010+1000

- The rebuilt app reopened as a standard `DOJO Today` window; the fullscreen control remained available and the window was not fullscreen.
- An existing recent-work object opened its own Details sheet. Details remained object-scoped and correctly showed no processing packet because that historical object predates the packet seam.
- One bounded non-sensitive composer test (`Reply READY only.`) reached the existing DOJO portal and returned `inference unavailable` for `gemma3:latest`.
- The surface showed `Blocked` / `DOJO portal reached · inference HOLD`; it created no Answer object, attached no processing packet, cleared no object state, and did not promote authority.
- The test status was dismissed and the composer value was restored to its prior state (`What Iou`). No object was saved or exported.
- This is runtime evidence for the inference HOLD and fullscreen correction. Runtime production of a complete `TodayProcessingPacket` remains unproven because the success branch did not occur.
