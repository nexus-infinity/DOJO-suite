# Inverse layer — manifestation requirement map

**State:** DESIGN_SEATED.HOLD. Proposal branch only.

Organic field = positive geometry (objects + relations with identity, position, state, semantics, time).
Inverse layer = potential geometry: the manifestation requirement map of the interval between two known objects.

Ask: what must exist between them for the relationship to be expressible, observable, and actionable in the digital field?

Requirement classes are questions, not install orders: sensor/observer, channel, timing, identity resolution, permissions, state storage, MCP/service capability as instrument, suite surface as portal, signal/response path.

Live-aligned: what can currently be perceived, spoken with, acted on, or is still missing.

Bridge without collapse: positive geometry → inverse map → Sitting 1 value chain tests the requirements → suite/cohabitational interface expresses what is presently lawful.
