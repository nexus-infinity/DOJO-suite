# FIELD Motion Lens Rack v0

**Object:** The FIELD of motion in this lane — not the pointers that look at it.
**Status:** `SEATED.PROCESS` · course named for bounded deliveries · not production spin
**Mission:** `DOJO-FIELD-MOTION-LENS-RACK-V0`
**KC packet:** `/Users/field/◎Kings-Chamber/chronicle/KC_REENTRY_20260831T005312Z_DOJO-FIELD-MOTION-LENS-RACK-V0.receipt.json`
**Overview lens:** `metamorphosis`
**Controlling:** `kingsman`

**Not:** a new lens · a new FIELD · Gallery 27 · MapKit · CarPlay runtime · byte-copy RAG · bobsled production seal

---

## What they point at

The glasses below are amplifiers. They do not become the terrain.

This lane’s field:

```text
Today          — ordinary native Mac work (Capture → object → local receipt)
Mac OS depth   — PARTIAL (window seated; connectors thin)
CarPlay        — CHANNEL-HELD (driving-safe sibling)
Landscape/place— HOLD.LandscapeMappingUnseated (where the human is)
Research       — AKRON-admit, pointer-only
```

Motion in that field is a mini Observer → Architect → Weaver cycle, then return to Do. Stalling on pointer-selection after intention is named is not caution; it is standing still.

---

## Registered rack (no invented lens_ids)

| Job on the field | Lens | Amplifier |
|---|---|---|
| Identity stays while form changes | `metamorphosis` | Seat invariants first; ski-line primes unlock the next hill |
| Stations, layering, octave | `french_cuisine` | Mise → heat → plate → clean-down; Do … Ti → Do′ |
| Return to a centred stance | `universal_dynamic_neutral` | Every turn lands before the next is admitted |
| Living flows, edges, yield | `permaculture` | Read water/capacity before cutting a channel |
| Terrain, high ground, timing | `art_of_warfield` | Know the ground before drawing a map |
| One object, stop, receipt | `kingsman` | Call contract; no unknown identity |
| Quiet surface | `surface_decorum` | Depth reachable; first screen stays ordinary |
| After the course is named | `bobsled_optimal_run` | Exploit the track; do not invent it |
| One bounded delivery | `cricket` | Scorecard, commentary ≠ play, next ball |

**CSIA** is a teaching phenotype, not a registered `lens_id`. Skill-stack unlock = Morphosis ski-line. Three-phase turn that always recentres = `universal_dynamic_neutral`. Same function as Do′.

**Channel HOLDs are the base-skill slope.** Holding Today, CarPlay, place, research, and Mac-OS depth as separate named channels — with lineage (what each is, what it must not become) — is the CSIA drill, not postponement. Edge control before the next hill. Lineage is the stance: you cannot ski if you do not know which run you are on.

Canon: `◎Kings-Chamber/config/DOJO_LAWFUL_AFFORDANCE_PROGRESSION_SKI_LINE_V0.md`
Amplifier seating: CSIA recursive skill-building = base before re-entry; three-phase turn = initiation → shaping → completion/neutral.

```text
LOOK ↔ ASK → NAME → PLACE → TIME → RELATE → CHECK → TRY → RETURN
```

On this slope, TRY is the ordinary Today loop (Capture → object → receipt). Later hills (CarPlay runtime, MapKit, chamber spin) stay HOLD until those primes are stable and receiptable. Expert flow may compress the scaffold; it must not skip unproven stages.

**Art of water** is not a registered `lens_id`. Water and regeneration = `permaculture`. Topographical mapping = `art_of_warfield`. Together they intimate the field. They do not become a map app.

Canonical octave (do not substitute):
`◎Kings-Chamber/config/FRENCH_CUISINE_OCTAVE_CROSSWALK_QUEEN_GRAND_ARKADAS_CALLFILE_S011_KC_V0.md`

Ski-line primes:
`◎Kings-Chamber/config/DOJO_LAWFUL_AFFORDANCE_PROGRESSION_SKI_LINE_V0.md`

---

## Octave as one turn (then back to Do)

CSIA’s initiation / shaping / completion is the same cycle as the French cuisine octave, spoken in ski. It is not a second authority.

| Phase | Octave | On this field |
|---|---|---|
| Initiation (neutral → edge) | **Do–Re** | Name ground. See relations. Do not yet cut the hill. |
| Shaping (skill under load) | **Mi–Fa–Sol** | One object. Boundary. Taste / arbitrate. Mini OAW weaves only that object. |
| Completion → new neutral | **La–Ti–Do′** | Plate the artifact. Record tension. Receipt. Return surplus. Next ball from new Do — not from a lean. |

```text
Do  mise / dynamic neutral
Re  relations / topography / flows
Mi  one dish, one object
Fa  heat and HOLD
Sol taste / King’s Chamber if truth competes
La  plate in the lawful home
Ti  chronicle the leftover tension
Do′ clean down — new ground, not erased reset
```

Base skills unlock the next terrain. A tool that appears before its primes is a fall, not progress.

---

## Cook card — one recipe, one intended attribute

French cuisine is the **layering grammar**. The octave is the **cycle**. Other lenses season only the station they belong to. They do not all sit in the pot.

Rule: **one recipe intention cooks one attribute.** Garnish is not the dish. A second attribute is a second recipe, after Do′.

Do not send a Today sitting through every FIELD chamber. The octave here is kitchen action. Chamber names in the canonical crosswalk stay chamber work.

| Note | Kitchen | Cook this sitting’s attribute | Flavour only if needed |
|---|---|---|---|
| **Do** | Mise / receive | What is already on the board? Ingredients, HOLDs, Unknowns. Do not shop mid-recipe. | `permaculture` (capacity) · `kingsman` (call) |
| **Re** | Stations | How do existing pins relate? Keep them separate. | `art_of_warfield` · `metamorphosis` (identity of what already sits) |
| **Mi** | Name the dish | Write **recipe intention** and **intended attribute** in one sentence each. Authority ceiling. Forbidden garnish. | `kingsman` · `surface_decorum` |
| **Fa** | Heat / pass | Primes present? Unlock, WAIT, or HOLD. Mini Observer / Architect here. | ski-line · `universal_dynamic_neutral` |
| **Sol** | Taste | Does the cook match the declared attribute? Competing truth → King’s Chamber. | `kingsman` |
| **La** | Plate | Weaver puts **only that attribute** in its lawful home. Declared vs actual. | `cricket` (this ball) |
| **Ti** | Hold service if incoherent | Leftover tension, HOLDs, what did *not* cook. Commentary is not the plate. | Chronicle / local receipt |
| **Do′** | Clean down | Receipt. Return surplus. New ground. Next recipe from centre, not from a lean. | `universal_dynamic_neutral` · `bobsled_optimal_run` (same course) |

Fill this before heat. If Mi is blank, do not turn on Fa.

```text
Recipe intention:     ________________________________
Intended attribute:   ________________________________
Not this dish:        ________________________________
Do  (on the board):   ________________________________
Re  (relations):      ________________________________
Mi  (name + ceiling): ________________________________
Fa  (UNLOCK/WAIT/HOLD): ______________________________
Sol (matches intention?): ____________________________
La  (plated where):   ________________________________
Ti  (leftover):       ________________________________
Do′ (next recipe):    ________________________________
```

Worked plate — last sitting:

```text
Recipe intention:   Name landscape/place as a held channel so the field is visible.
Intended attribute: Place exists as HOLD.LandscapeMappingUnseated, not as a map.
Not this dish:      MapKit, CarPlay nav, Today first-screen map.
Do:                 Today shell; CarPlay CHANNEL-HELD; periscope pin empty.
Re:                 Place ≠ CarPlay ≠ Today ≠ ATLAS.
Mi:                 Landscape/place channel contract + snapshot.
Fa:                 Initiation only — UNLOCK the name, HOLD the tools.
Sol:                Snapshot flags false; inspector and G6 show HOLD.
La:                 docs + LandscapePlaceSnapshot + inspector + lab cards.
Ti:                 Live location still Unknown; GUI click-through not run.
Do′:                Next recipe is one named cricket delivery.
```

Efficiency: skip a flavour lens when that station is already clear.
Effectiveness: Sol fails if La plated a different attribute than Mi named.

---

## Bobsled delivering cricket

Same field; two glasses.

- **Bobsled** — the course for a sitting is named and held. Drivers run it. They do not redesign the track mid-run. Speed is a consequence of precision.
- **Cricket** — one ball is one bounded delivery: object, boundary, weaver, scorecard, receipt. Commentary is not play. Appeal is not a wicket until replay.

```text
Recipe intention named (Mi)
    → Cook card through Do … Do′  (OAW lives inside Fa–La)
    → Receipt
    → Next recipe / cricket delivery from Do′
```

Do not wait for a perfect 360 before the first ball. Do not skip the mini cycle. Do not start a bobsled clock on an unnamed course (MapKit, CarPlay runtime, and research RAG remain unsealed).

---

## Scorecard (this innings)

**Delivery:** CSIA base-skill TRY — Capture evidence journal, same path Keep uses.
**Pin:** `logs/state_patches/today_capture_base_skill_runs_v0.md`

Prior landscape initiation remains seated: `docs/DOJO_LANDSCAPE_PLACE_CHANNEL_CONTRACT_V0.md`.

**Next ball (after Do′)**

Lived: open DOJO Today → Capture → Keep → Proof. That is the inverse-field run of the same journal.
