# Proposal — geometry first, then translation

**Branch:** `proposal/geometry-first-surfaces-20260917`  
**Base:** `main` @ `283078b` (PIPELINE_CONVERGENCE_20260917)  
**State:** PROPOSAL. Not runtime. Not merge. Not spin.  
**Reflection rule:** every later commit on this branch must open Geometry → Translation → Phenotype. If those three cannot be written, HOLD.

This packet seats ideas from the 2026-09-17 sitting so they can be checked for coherence against higher geometry before they become targets, MDM, or Watch chrome.

---

## Geometry (do not skip)

1. Sovereign work the operator created is owed a version on Local Sovereign Field. Vendor stores (Notion, Raycast, Apple apps) are representations.
2. ◼︎ DOJO is the spinning top. DOJO suite is observer-aligned send/receive portals into that top. Suite ≠ infra at every address the observer sleeps.
3. PULSE is house signalling. Murmur is coordination packets. MDM is return-to-known-good on the Apple plane. They are three objects.
4. Alignment is always geometry first, then translation into a field of *technology* or a field of *person*. Person is not a device. Device is not the top.
5. Full stack on a body means full *understanding and lawful lock* of that body's surfaces — not full power.
6. Deterministic infrastructure is judged by *experience represented*, not by cloning a vendor. How did Notion get built, and can we host that experience more efficiently — not how do we build Notion.

```text
hardware genotype
  → attribute register (chip, last signed OS, sensors that exist)
  → MDM baseline + role layer
  → murmur node id
  → hybrid capability tier L0–L4 under Comfort/Work pins
  → locked surfaces for this body
```

Software weather (Apple Intelligence, Foundation Models, new watchOS APIs) is a **held channel** even when this body will never run it. Absence on silicon is not deletion of the lane.

---

## Test — deterministic infrastructure vs Notion experience

Wrong test: How do we build Notion?
Right test: How did Notion get built? What experience did that produce? How do we represent the same experience, or a better one, with less tax on attention and money?

Experience-surface named by the operator: look-at-it page + living database + work retained in a user-fronted workspace (including while looking at the page).

Infra **passes** only if that experience can be hosted on LSF / suite portals without becoming a Notion rebuild, a credit meter, or a Settings mall.
Infra **fails** if it answers “blocks / RAG / MCP implemented” while the operator cannot recreate the page in view, keep the work, or leave without a hook.

This test applies to Watch, MDM, and every later target on this branch. Implementation details are translation. The experience is geometry.

---

## Translation — what already exists (do not rebuild)

| Object | Home | State |
|---|---|---|
| Bounded suite implementation | `main` `283078b` + `docs/PIPELINE_CONVERGENCE_20260917.md` | PROMOTE.BOUNDED_IMPLEMENTATION; devices/authority HOLD |
| Apple six surfaces | Notion *Apple Required Surface Matrix v0* (APPROVE specimen 2026-08-01) | macOS · iPhone · iPad · Watch · CarPlay · Apple TV; runtime not admitted |
| Hybrid capability | Notion *Quantisation & Hybrid Capability Registry* | L0–L4; one row (Studio); quality gate per tier |
| MDM / murmur treaty | Notion *Dojo Field Protocol — Managed Devices* | design seated; loop not live |
| Watch law | Notion APPle-001-watchOS v1.1 + local `WATCH_ULTRA_MURMUR_ARCHITECTURE_SPEC_V0.md` | Ultra 2 host; ceiling `HAPTIC_CUE_ONLY` |
| Watch sources | `Sources/DOJOWatchApp` | two files; **not** in the eight passing schemes |
| Linear | Field-MacOS-DOJO-suite | BER-69 witness seated 2026-09-17 |

`main` passing schemes: DOJOApp, ArkadasApp, DOJOiOSApp, DojoLinkApp, OB1LinkApp, DOJOShared, DOJOUI, DOJOPersistence. No Watch, tvOS, or CarPlay scene in that list.

---

## Proposed development (this branch only)

Work is ordered. A later item does not start because an earlier one is interesting.

### P0 — Keep the reflection surface
This file. Re-read Geometry before any target change. Do not merge to `main` until a named receipt says the translation still matches geometry.

### P1 — First body: Apple Watch Ultra 2
- Add `DOJOWatchApp` to the XcodeGen graph so it *builds* with the suite.
- Phenotype: one complication + haptic policy stub. Own app = glance / cue, not a drawer.
- Orchestrate Apple channels (Workout, HealthKit, Smart Stack) without rebranding watchOS.
- Clutter: hide unused vendor apps via MDM / Focus on this device — not a new LLM.
- Participation ceiling: **L0**. TinyLlama / on-wrist discovery classifier stays historical v1.0, not this branch.
- Software channel Apple Intelligence / on-device Siri: PRESENT-or-ABSENT held, unused.

### P2 — Name missing Apple surface *targets* (skeletons only)
- Named iPad destination (may share iOS binary).
- CarPlay scene on `DOJOiOSApp` (parked card; vehicle control forbidden).
- `DOJOTVApp` stub (room card; not PULSE infrastructure).
- Old iPads: role `PULSE.display` on last signed iPadOS; not suite iOS 17.

### P3 — Attribute topography before fleet enroll
Hardware-first columns, not a serial dump:
`device_model → chip → max_signed_os → participation_ceiling → software_channels_held → role`.
Then one device may receive murmur id + MDM baseline name. House-wide enrollment HOLD.

### P4 — Raycast lesson as constraint
Attributes plug under the hood. No Settings mall. Copartner holds the difference; operator sees one next move.

### Parked
- Notion corpus version onto LSF (`SUITE.SURFACE.LSF_SOVEREIGN_VERSION`) — owed, not this branch's weave.
- Architecture-finish / cheap-capable-system proof.
- KC live route-admission fixtures (named on convergence receipt).
- Training / L4 on any suite device.

---

## Coherence checks (use on every return)

1. Geometry still first? Or did a vendor API start defining the term?
2. Suite still a portal? Or did a target become the top?
3. Watch still haptic-only? Or did HealthKit become identity?
4. Old iPad still PULSE L0? Or did we install current suite on dead silicon?
5. Person field still distinct from tech field?
6. Held software channels still named, not deleted because unused?
7. Infra still judged by Notion-*experience* represented, not by Notion-*clone* completeness?

Fail any → HOLD. Do not add files to paper over the fail.

---

## Claim ceiling

Proposal branch. Does not amend `main`. Does not enroll devices. Does not raise murmur authority. Does not promote Apple Intelligence. Does not overwrite Notion canon or Linear.
