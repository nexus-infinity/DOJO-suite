# DOJO Landscape / Place Channel Contract v0

**Object:** Named place-in-the-landscape channel, sibling of CarPlay, not the Today application.
**Status:** `SEATED.CHANNEL` · `HOLD.LandscapeMappingUnseated`
**Morphosis:** form may appear (this pin); identity of Today, CarPlay, ATLAS, and Murmor stay unmoved.
**Not:** MapKit · Core Location tracking · Apple Maps clone · vehicle navigation · CarPlay runtime · ATLAS chamber UI · drone geospatial

**Parents:**
`docs/FIELD_MOTION_LENS_RACK_V0.md` ·
`docs/PERPLEXITY_SPINNING_TOP_GAP_ANALYSIS_V0.md` (periscope addendum) ·
`docs/CARPLAY_DOJO_CHANNEL_CONTRACT_V0.md` ·
`docs/DOJO_TODAY_IDENTITY_FORM_FUNCTION_BENCHMARK_V0.md`

**Snapshot:** `Sources/DOJOShared/Contracts/LandscapePlaceSnapshot.swift`

---

## Kingsman lens rack (this fill)

| Role | Lens | Job |
|---|---|---|
| Overview | `metamorphosis` | Seat invariants; let place arise as a phenotype, not a new being |
| Controlling | `kingsman` | One object, stop, receipt |
| Integration | `french_cuisine` | Place is a station, not the whole kitchen |
| Terrain | `art_of_warfield` | Name the ground before drawing a map |
| Surface | `surface_decorum` | Pin lives under the hood / lab, not as Today first-screen map |
| Invitation | `positive_constraint_invitation` | Fill the space by holding it visibly |

**Ski-line:** this sitting is **initiation** (name the terrain). Map tools, live location, and CarPlay templates remain locked until primes exist.

Governing motion rack (octave, CSIA phenotype, permaculture, topography, then bobsled/cricket): `docs/FIELD_MOTION_LENS_RACK_V0.md`. Place is one pin in that field, not the field.

---

## Mini OAW (every implementation step)

```text
Observer   —  what exists, what is HOLD, what must not collapse
Architect  —  one object, boundary, acceptance
Weaver     —  change only that object in its lawful home
Receipt    —  declared vs actual
Return     —  periscope still sees separate pins
```

Do not wait for a perfect 360 before the first fill. Do not skip the mini cycle.

---

## Invariants (do not morph away)

- Today remains the ordinary Mac work surface.
- CarPlay remains the driving-safe channel (`CHANNEL-HELD`).
- Place / mapping is **where the human is in the world**.
- Research / grounding remains AKRON-admit, pointer-only.
- Place is not vehicle control, not climate, not music.

---

## Channel

| Pin | Value |
|---|---|
| Name | Landscape / place |
| State | `HOLD.LandscapeMappingUnseated` |
| Observer | JB in the world (desk, walk, car, room) |
| Mode | reporting pin now; glanceable place cue later |
| Allowed now | Name the space · keep it visible · keep it separate |
| Later (after primes) | Bounded place cue · MapKit under a new sitting · CarPlay-safe place glance as a *CarPlay* sitting |

## Not claimed

- live coordinates
- geocoding
- turn-by-turn
- MapKit entitlement
- location permission
- Perplexity maps integration
- ATLAS as a map app

## HOLD triggers

- `HOLD.LandscapeMappingUnseated`
- `HOLD.MapKitNotAuthorized`
- `HOLD.LocationPermissionUnknown`
- `HOLD.CollapseIntoCarPlay`
- `HOLD.CollapseIntoTodayMap`

## Mini-cycle result (this sitting)

Observer: space was named in the periscope and empty.
Architect: initiation = contract + snapshot + inspector + lab card.
Weaver: those artifacts.
Acceptance: `placeKnown == false`, `mapRuntime == false`, `carPlayCollapsed == false`.
